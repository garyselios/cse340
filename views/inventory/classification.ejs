<!-- Content Heading -->
<% if (title) { %>
<h1><%= title %></h1>
<% } else { 
  res.redirect('/')
} %>

<!-- Message Block (disabled for now) -->
<%# messages() %>

<!-- Inventory Display -->
<%- grid %>